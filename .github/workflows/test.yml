name: CI on-test-pr-events

on:
  pull_request:
    types:
      - labeled
      - unlabeled

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo $GITHUB_SHA
      - run: echo $GITHUB_REF
